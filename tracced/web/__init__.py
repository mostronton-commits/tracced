"""web — сторінка над двигуном early: встав контракт → графік капи → вікно вручну → таблиця.

aiohttp + Jinja2, без фронтенд-фреймворків. Один робочий потік для аналізів (ліміт Solana
Tracker спільний), результати зберігаються в output/early/web/. Порт відкривається лише на
127.0.0.1 (docker-compose.yml). Пароль — тільки якщо задано WEB_PASSWORD.
"""
