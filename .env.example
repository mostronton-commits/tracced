# Solana Tracker Data API key (required): https://www.solanatracker.io/data-api
SOLANATRACKER_API_KEY=
# Optional: protect the page with a password (needed on a public server)
WEB_PASSWORD=
# Random string that signs the beta and wallet sign-in cookies. Without it every restart signs everyone out.
# Make one with: openssl rand -hex 32
WEB_SECRET=
# Optional: the domain wallets sign in to, if the proxy rewrites the Host header (defaults to the request host)
WEB_DOMAIN=
# Optional: wallet addresses (comma-separated) that can open /admin — who connected and what they did
ADMIN_WALLETS=
# Optional: AI agent (OpenAI-compatible chat endpoint; Ollama Cloud by default)
ASSISTANT_KEY=
ASSISTANT_URL=https://ollama.com/v1
ASSISTANT_MODEL=kimi-k2.6:cloud
# Optional: a Solana RPC with full signature history for wallet age / funder (mainnet-beta by default)
SOLANA_RPC_URL=
# Optional: Umami Cloud analytics (https://umami.is); the id is public by design
UMAMI_WEBSITE_ID=
